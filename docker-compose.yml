services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: peacock
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD}
      POSTGRES_DB: peacock
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U peacock -d peacock"]
      interval: 5s
      timeout: 3s
      retries: 10
volumes:
  postgres_data:
